# AGENTS.md — all 技能

本目录为实现 `all` 技能的 `SKILL.md`（全流程收敛编排：解析任务→判定所需技能集合→
反复调用本目录技能循环迭代直至收敛为最佳，含收敛判据硬闸门与迭代守卫）。
SKILL.md 为唯一权威内容，勿复制改动。执行 all 技能时在当前工作目录生成
`.all.<时间戳>.log` 会话日志（任务定义、技能选择、每轮迭代与收敛评估全程追加记录），
不入库。
