---
name: autoresearch-goal
description: Sunset stub — use $autoresearch instead
---

# Autoresearch-goal — removed

`$autoresearch-goal` was removed in OMX 0.21; use `$autoresearch` instead.

Autoresearch-goal was a duplicate research-over-goal-mode entry point. `$autoresearch` is the single canonical validator-gated research loop; it owns the research mission lifecycle that autoresearch-goal duplicated.

Migration: replace `$autoresearch-goal` with `$autoresearch`.

Task: {{ARGUMENTS}}
