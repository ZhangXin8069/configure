# AGENTS.md — up 技能

本目录为实现 `up` 技能的 `SKILL.md`（技能库升级：本地基线调查→GitHub 最佳实践调研
（连接失败重试至成功）→差距分析→优化/补充/新增/debug→验证→收敛评估）。
SKILL.md 为唯一权威内容，勿复制改动。执行 up 技能时在终端输出基线、调研、差距、改动与验证摘要；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
