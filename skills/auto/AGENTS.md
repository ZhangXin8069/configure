# AGENTS.md — auto 技能

本目录为实现 `auto` 技能的 `SKILL.md`（全自动前缀执行：`~auto-<技能名> <任务>`，
启动时一次性预授权权限与配置，此后 0 交互，自动循环调用其他技能直至收敛，除非强制中断）。
SKILL.md 为唯一权威内容，勿复制改动。
执行 auto 技能时只输出预授权、轮次和收尾摘要；有 Git 改动时按上级 `AGENTS.md` 公共契约检查、只暂存本次文件并创建本地 commit，不 push；无 Git 或无改动立即跳过。
