# AGENTS.md — auto 技能

本目录为实现 `auto` 技能的 `SKILL.md`（全自动前缀执行：`~auto-<技能名> <任务>`，
启动时一次性预授权权限与配置，此后 0 交互，自动循环调用其他技能直至收敛，除非强制中断）。
SKILL.md 为唯一权威内容，勿复制改动。执行 auto 技能时在当前工作目录生成
`.auto.<时间戳>.log` 会话日志（预授权清单、每轮迭代、停滞/中断/收尾记录全程追加），不入库。
