# AGENTS.md — debug 技能

本目录为实现 `debug` 技能的 `SKILL.md`（调试与问题定位，含 frontmatter 与完整工作流）。SKILL.md 为唯一权威内容，勿复制改动。执行 debug 技能时在终端保留复现、定位和验证摘要；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
