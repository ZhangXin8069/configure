# AGENTS.md — skill-creator 技能

本目录为实现 `skill-creator` 技能的 `SKILL.md`（技能创建与优化：意图捕获→草稿→测试评估→迭代→触发描述优化，吸收 Anthropic 官方 skill-creator 与 Agent Skills 分级披露规范）。SKILL.md 为唯一权威内容，勿复制改动。执行 skill-creator 技能时在终端输出意图、评估、迭代与触发检查点；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
