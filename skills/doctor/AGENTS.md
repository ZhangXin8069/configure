# AGENTS.md — doctor 技能

本目录为实现 `doctor` 技能的 `SKILL.md`（广域健康检查：覆盖度、镜像、一致性、入口、权限、
语法和基础安全边界；默认只诊断不改）。SKILL.md 为唯一权威内容，勿复制改动。执行 doctor
技能时在终端保留盘点、差异与风险摘要；有 Git 改动时按上级 `AGENTS.md` 公共契约执行
`git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
