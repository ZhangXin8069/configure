# AGENTS.md — analy 技能

本目录为实现 `analy` 技能的 `SKILL.md`（整体分析参考层：只读分析当前 git 库全部文档与代码，
建立主体结构/部分关系/项目思路、入口依赖与证据索引，解析 `{$用户输入}` 主题，结论附
`文件:行号` 参考源，输出供后续 agent 使用的 PDF 到工作目录 docs/，含 frontmatter 与完整工作流）。
SKILL.md 为唯一权威内容，勿复制改动。
执行 analy 技能时通过终端输出和报告文件保留证据；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
