# AGENTS.md — optim 技能

本目录为实现 `optim` 技能的 `SKILL.md`（性能/效率优化，先测量后优化、基准驱动、含 frontmatter 与完整工作流）。SKILL.md 为唯一权威内容，勿复制改动。执行 optim 技能时在终端输出基线、迭代测量和收益摘要；有 Git 改动时按上级 `AGENTS.md` 公共契约检查、只暂存本次文件并创建本地 commit，不 push；无 Git 或无改动立即跳过。
