# AGENTS.md — tag 技能

本目录为实现 `tag` 技能的 `SKILL.md`（git 四类标签 stab/dev/bug/test 管理，含 frontmatter 与完整操作手册）。SKILL.md 为唯一权威内容，勿复制改动。执行 tag 技能时只使用 Git 提交、标签和终端结果；有 Git 改动时按上级 `AGENTS.md` 公共契约检查、只暂存本次文件并创建本地 commit，不 push；无 Git 或无改动立即跳过。
