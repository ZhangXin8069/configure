# AGENTS.md — tag 技能

本目录为实现 `tag` 技能的 `SKILL.md`（git 三类标签 stab/dev/bug 管理，含 frontmatter 与完整操作手册）。SKILL.md 为唯一权威内容，勿复制改动。目录内 `.agent.*.log` 为技能创建过程日志，不入库；执行 tag 技能时在当前工作目录生成 `.tag.<时间戳>.log` 会话日志（操作、确认、push/重试记录），不入库。
