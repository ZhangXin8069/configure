# AGENTS.md — all 技能

本目录为实现 `all` 技能的 `SKILL.md`（全流程收敛编排：解析任务→判定所需技能集合→
反复调用本目录技能循环迭代直至收敛为最佳，含收敛判据硬闸门与迭代守卫）。
SKILL.md 为唯一权威内容，勿复制改动。
执行 all 技能时只在终端输出阶段结果；
仅在终端输出阶段结果；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
