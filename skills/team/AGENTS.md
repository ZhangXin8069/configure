# AGENTS.md — team 技能

本目录为实现 `team` 技能的 `SKILL.md`（长期协作编排：worker 调度、状态机、工作树隔离、健康检查、失败重派发与收敛收尾）。SKILL.md 为唯一权威内容，勿复制改动。执行 team 技能时在终端输出状态机、worker 摘要与收敛结果；有 Git 改动时按上级 `AGENTS.md` 公共契约执行 `git diff --check` 与定向复查；未经用户明确要求不自动暂存、提交或推送；无 Git 条件立即跳过。
