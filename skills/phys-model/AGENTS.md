# AGENTS.md — phys-model 技能

本目录为物理建模与数值落地技能的实现目录，`SKILL.md` 是唯一权威内容。
该技能面向从物理问题到可计算模型的闭环：方程、量纲/对称性、离散化、求解器与验证。
遇到仓库结构问题先用 `analy`，遇到已有核心细节穷尽先用 `pure`，数值验证用 `test`。
执行时遵循上级 `AGENTS.md` 公共契约；有 Git 改动时按约定做 `git diff --check` 与定向复查。
不要在此目录放与技能无关的资源。
